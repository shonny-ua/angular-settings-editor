import { FindEditorPipe } from './find-editor.pipe';

describe('FindEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new FindEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
